# Enviroment variables needed to connect to the LiveKit server.

# Environment variables needed to connect to the LiveKit server.
# Copy this file to `.env.local` or `.env` and fill in your real credentials.
# IMPORTANT: Do NOT commit your real `.env` file to source control. `.env*` is ignored by
# the project's `.gitignore` but `.env.example` is intentionally tracked as a template.

# LiveKit connection URL (use wss:// for LiveKit cloud or https:// for http endpoints)
LIVEKIT_URL=https://your-livekit-server-url

# LiveKit API credentials
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# Internally used environment variables (optional)
NEXT_PUBLIC_APP_CONFIG_ENDPOINT=
SANDBOX_ID=

# Example (for your local .env only — DO NOT commit these values):
# LIVEKIT_URL=wss://ogatalk-22i6cx1x.livekit.cloud
# LIVEKIT_API_KEY=APIwb7xF3FGmeDL
# LIVEKIT_API_SECRET=lce1ZmzJnv7UBCrmOpYeNzHZTJ62m0s3RFTJCqQt2cO
